apiVersion: v1
kind: Service
metadata:
  labels:
    app: zz_frame-test
  name: zz_frame-test
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    app: zz_frame-test
  type: ClusterIP
status:
  loadBalancer: {}
